plugins {
    id("io.github.erudys27.oepm")
}

oepm {{{PROJECT_ROOT_BLOCK}}
}

// Registries live in oepm-registries.properties, at the ABL project root
// (see projectRoot above, if set) - not here. See oepm-tool's README.md.
// Run `oepm registry add <prefix> <url>` to add one.
