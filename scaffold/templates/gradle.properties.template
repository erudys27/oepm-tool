# Where the oepm plugin's source lives, for includeBuild during local dev.
# Computed relative to wherever oepm-tool was actually cloned when this
# project was scaffolded. Override if you move either clone.
oepmToolPath={{OEPM_TOOL_PATH}}
